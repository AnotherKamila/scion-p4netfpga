# TODO should this Makefile exist?
TOP=../../..
include $(TOP)/Makefile.inc

### Settings #################################################################
include ../Makefile.inc
include Makefile.inc

### User-facing targets ######################################################

all: ## TODO

build: ## TODO

clean: ## Remove generated files
	make -C ${HW} distclean
	make -C ${SW} distclean
	rm -rfv vivado*

### Targets that actually do stuff ###########################################
